#include <stdio.h>

int main()
{
  int length, breadth;

  printf("Length of Rectangle is: ");
  scanf("%d", &length);

  printf("Breadth of Rectangle is: ");
  scanf("%d", &breadth);

  printf("Area = %d", length * breadth);

  return 0;
}
